{
  "category": [
    "Express",
    "Node.JS"
  ],
  "date": "2021-08-03T13:31:23.631Z",
  "title": "cvcvchvuvjh",
  "slug": "cvcvchvuvjh",
  "main_img": "/static/images/blog/screenshot_4.png",
  "body": "The file widget allows editors to upload a file or select an existing one from the media library. The path to the file will be saved to the field as a string.\n\n* **Name:** `file`\n* **UI:** file picker button opens media gallery\n* **Data type:** file path string\n* **Options:**\n\n  * `default`: accepts a file path string; defaults to null\n  * `media_library`: media library settings to apply when a media library is opened by the current widget\n\n    * `allow_multiple`: *(default: `true`)* when set to `false`, prevents multiple selection for any media library extension, but must be supported by the extension in use\n    * `config`: a configuration object that will be passed directly to the media library being used - available options are determined by the library\n    * `media_folder` (Beta): file path where uploaded files will be saved specific to this control. Paths can be relative to a collection folder (e.g. `files` will add the file to a sub-folder in the collection folder) or absolute with reference to the base of the repo which needs to begin with `/` (e.g `/static/files` will save uploaded files to the `static` folder in a sub folder named `files`)\n    * `choose_url`: *(default: `true`)* when set to `false`, the \"Insert from URL\" button will be hidden",
  "bodyHtml": "<p>The file widget allows editors to upload a file or select an existing one from the media library. The path to the file will be saved to the field as a string.</p>\n<ul>\n<li>\n<p><strong>Name:</strong> <code>file</code></p>\n</li>\n<li>\n<p><strong>UI:</strong> file picker button opens media gallery</p>\n</li>\n<li>\n<p><strong>Data type:</strong> file path string</p>\n</li>\n<li>\n<p><strong>Options:</strong></p>\n<ul>\n<li>\n<p><code>default</code>: accepts a file path string; defaults to null</p>\n</li>\n<li>\n<p><code>media_library</code>: media library settings to apply when a media library is opened by the current widget</p>\n<ul>\n<li><code>allow_multiple</code>: <em>(default: <code>true</code>)</em> when set to <code>false</code>, prevents multiple selection for any media library extension, but must be supported by the extension in use</li>\n<li><code>config</code>: a configuration object that will be passed directly to the media library being used - available options are determined by the library</li>\n<li><code>media_folder</code> (Beta): file path where uploaded files will be saved specific to this control. Paths can be relative to a collection folder (e.g. <code>files</code> will add the file to a sub-folder in the collection folder) or absolute with reference to the base of the repo which needs to begin with <code>/</code> (e.g <code>/static/files</code> will save uploaded files to the <code>static</code> folder in a sub folder named <code>files</code>)</li>\n<li><code>choose_url</code>: <em>(default: <code>true</code>)</em> when set to <code>false</code>, the \"Insert from URL\" button will be hidden</li>\n</ul>\n</li>\n</ul>\n</li>\n</ul>\n",
  "dateFormatted": "August 3, 2021"
}